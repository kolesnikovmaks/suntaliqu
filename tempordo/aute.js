/**
 * This function calculates the better-performing ad between two given ads.
 * The comparison is based on click-through rate (CTR) and conversion rate (CR).
 * 
 * @param {Object} firstAd - The first ad's performance metrics.
 * @param {Object} secondAd - The second ad's performance metrics.
 * @param {string} campaignId - The ID of the campaign.
 * @param {string} adGroupId - The ID of the ad group.
 * @returns {Object} - The ad with better performance metrics.
 */
function getBetterPerformingAd(firstAd, secondAd, campaignId, adGroupId) {
    // Your implementation here...
}
