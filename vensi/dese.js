// Assuming 'this._' refers to an instance of Lodash or Underscore library

// Alias Lodash as '_'
var _ = this._;

// Now you can use '_' as a shorthand for Lodash functions
var result = _.map([1, 2, 3], function(num) {
  return num * 3;
});
